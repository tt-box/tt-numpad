{
  "name": "tt-numpad",
  "version": "0.0.1",
  "description": "Cash register Num Pad",
  "main": "index.html",
  "repository": {
    "type": "git",
    "url": "https://github.com/tt-box/tt-numpad.git"
  },
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "polymer lint --input tt-numpad.html",
    "dev": "polymer serve -p 8080 & npm run watch",
    "test": "polymer test",
    "watch": "browser-sync start --proxy localhost:8080 --startPath '/components/tt-numpad/demo/' --files '*.html, *.js, demo/*' "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.16.0"
  }
}
